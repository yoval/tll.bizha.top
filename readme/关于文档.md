> SHOW CREATE TABLE ads_dbs_report_food_di

"Table"	"Create Table"
"ads_dbs_report_food_di"	"CREATE TABLE `ads_dbs_report_food_di` (
  `business_date` varchar(255) NULL COMMENT '日期',
  `stat_shop_id` varchar(255) NULL COMMENT '门店编号',
  `stat_shop_name` varchar(255) NULL COMMENT '门店名称',
  `license_person` varchar(255) NULL COMMENT '法人名称',
  `prov_id` varchar(255) NULL COMMENT '省份编码',
  `prov_name` varchar(255) NULL COMMENT '省份名称',
  `city_id` varchar(255) NULL COMMENT '城市编码',
  `city_name` varchar(255) NULL COMMENT '城市名称',
  `city_level` varchar(255) NULL COMMENT '城市等级',
  `district_id` varchar(255) NULL COMMENT '区县编码',
  `district_name` varchar(255) NULL COMMENT '区县名称',
  `busi_area_type` varchar(255) NULL COMMENT '商圈',
  `region_manager_name` varchar(255) NULL COMMENT '大区经理',
  `prov_manager_name` varchar(255) NULL COMMENT '省经理',
  `district_manager_name` varchar(255) NULL COMMENT '区域经理',
  `total_amount` decimal(20, 2) NULL COMMENT '流水金额',
  `pay_amount` decimal(20, 2) NULL COMMENT '实收金额',
  `report_amount` decimal(20, 2) NULL COMMENT '报货金额',
  `orange_report_cnt` bigint(20) NULL COMMENT '橙子报货数量（15kg/件）',
  `is_orange_report` int(11) NULL COMMENT '橙子是否报货',
  `lemon_report_cnt` bigint(20) NULL COMMENT '柠檬报货数量（15kg/件）',
  `is_lemon_report` int(11) NULL COMMENT '柠檬是否报货',
  `report_amount_last_day` decimal(16, 2) NULL COMMENT '上一天报货金额',
  `report_amount_last_week` decimal(16, 2) NULL COMMENT '上周同期报货金额',
  `report_amount_last_year` decimal(16, 2) NULL COMMENT '去年同期报货金额',
  `load_time` varchar(255) NULL COMMENT '数据更新时间'
) ENGINE=OLAP
DUPLICATE KEY(`business_date`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`business_date`) BUCKETS 10
PROPERTIES (
""replication_allocation"" = ""tag.location.default: 3"",
""in_memory"" = ""false"",
""storage_format"" = ""V2"",
""disable_auto_compaction"" = ""false""
);"

